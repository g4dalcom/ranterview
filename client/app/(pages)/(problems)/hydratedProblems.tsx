import getQueryClient from '@/app/components/helper-components/getQueryClient';

const hydratedProblems = () => {
  const queryClient = getQueryClient();
  return <div></div>;
};

export default hydratedProblems;
